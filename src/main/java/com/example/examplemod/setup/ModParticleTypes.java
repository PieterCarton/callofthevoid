package com.example.examplemod.setup;

import net.minecraft.particles.BasicParticleType;
import net.minecraft.particles.ParticleType;
import net.minecraftforge.fml.RegistryObject;

import java.util.function.Supplier;

public class ModParticleTypes {
    /*
    public static RegistryObject<ParticleType<BasicParticleType>> SPARK = register("spark",
            () -> new BasicParticleType(false));

    public static RegistryObject<ParticleType<?>> register(String name, Supplier<ParticleType<?>> supplier) {
        return Registration.PARTICLE_TYPES.register(name, supplier);
    }
     */
}
